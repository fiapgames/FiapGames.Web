# Both point at the Kong API gateway — the single entry point for the system.
# Same address for docker-compose and for the kind cluster (see the README in
# FiapGames.Orchestration).
VITE_API_BASE_URL=http://localhost:8000
VITE_USERS_API_BASE_URL=http://localhost:8000
